# ==============================================================================
# Database Configuration
# ==============================================================================
# For SQLite (default - zero configuration, perfect for development):
DATABASE_URL=sqlite+aiosqlite:///./data/frames.db

# For PostgreSQL (better for production - supports concurrent writes):
# DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/frames_db

# ==============================================================================
# Application Settings
# ==============================================================================
APP_NAME=ImageFramesAPI
APP_VERSION=0.1.0

# Logging level: DEBUG, INFO, WARNING, ERROR, CRITICAL (case-sensitive!)
LOG_LEVEL=INFO

# Environment: development, staging, production
ENVIRONMENT=development

# ==============================================================================
# API Server Settings
# ==============================================================================
# Bind address (0.0.0.0 = all interfaces, 127.0.0.1 = localhost only)
API_HOST=0.0.0.0

# Server port
API_PORT=8000

# Auto-reload on code changes (development only)
API_RELOAD=true

# ==============================================================================
# Data Ingestion Settings
# ==============================================================================
# Path to CSV file for ingestion
CSV_FILE_PATH=./data/aiq_data.csv

# Number of rows to process per batch (higher = faster but more memory)
# Recommended: 500-1000 for optimal balance
CHUNK_SIZE=500

# ==============================================================================
# Security Settings
# ==============================================================================
# Admin token for protected endpoints (POST /frames/reload, DELETE /cache)
# IMPORTANT: Change this in production! Use a strong, random token.
ADMIN_TOKEN=changeme-secure-token-here

# ==============================================================================
# Performance & Caching Settings (Optional)
# ==============================================================================
# Cache TTL in seconds (how long to keep cached results)
# Default: 60 seconds
# CACHE_TTL_SECONDS=60

# Maximum size of single-frame cache (number of entries)
# Default: 1000
# FRAME_CACHE_SIZE=1000

# Maximum size of range-query cache (number of entries)
# Default: 100
# RANGE_CACHE_SIZE=100
